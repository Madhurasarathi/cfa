sudo apt-get install openssh-server
cd Download/
sudo chmod 600 labsuser.pem
sudo ssh -i labsuser.pem ubuntu@publicip
sudo apt-get update -y
sudo apt-get install apache2 php libapache2-mod-php php-mysql mysql-server mysql-client -y
sudo git clone <url>
cd cafedynamic/
sudo mkdir /var/www/html/cafedynamic
sudo cp -rf mompopcafe/* /var/ww/html/cafedynamic
sudo nano /etc/apache2/sites-available/000-default.conf * (edit document root)
sudo service apache2 restart
sudo mysql -u root -p
CREATE database cafedb;
CREATE user 'cafe'@'locahost' IDENTIFIED WITH musql_native_password BY 'msis@123';
GRANT ALL PRIVILAGES ON cafedb * TO 'cafe'@'localhost'WITH GRANT OPTIONS;
exit
cd cafedynamic/  ls   cd mompopdb/  ls (.sql)
sudo mysql -u cafe -p (user password)
show databases;  use cafedb;  source cafe-dbsql;  show tables;
select * from product;  exit;
cd /var/www/html/cafedynamic  ls cd mompopcafe/  ls
sudo nano getAppParameters.php (edit url:localhost, name:cafedb, user:cafe, password:msis@123
sudo service apache2 restart
sudo systemctl restart apache2
